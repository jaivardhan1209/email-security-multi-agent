# Core runtime — Microsoft Agent Framework is the multi-agent substrate.
agent-framework-core>=1.17.0,<2
agent-framework-ollama>=1.17.0,<2   # local open-source model runtime (recommended POC path)
pydantic>=2.9
httpx>=0.27
python-dotenv>=1.0

# Web inspector UI (app is fully usable from the CLI without these)
fastapi>=0.115
uvicorn[standard]>=0.30

# Dev / test
pytest>=8.0
pytest-asyncio>=0.24
